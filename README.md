full stack, fully functional node.js job board

npm run dev to start the app with the backend
add config.env file in congif/ dir

PORT=5000
MONGODB_URI="you mongoeb database string"
JWTSECRECT="yoursecrectcode"